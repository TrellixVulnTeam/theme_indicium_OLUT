{{!< default }}

<main class="index bg-gray-100" style="overflow: hidden">
  {{> header}}
  {{> recent_post}}
  
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mx-20">
    {{#foreach posts}}
      <div class="post-card {{#if @first}}hidden{{/if}} hover:shadow-xl transform hover:scale-105 duration-500">
        {{> post_cards}}
      </div>
    {{/foreach}}
</div>
</main>
<div class="pt-5 pb-10">
{{pagination}}
</div>
<div>
    {{> newsletter}}
</div>